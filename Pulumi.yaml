name: infra-app
runtime:
  name: python
  options:
    virtualenv: venv
description: Air-tek technical assessment
template:
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-west-2
